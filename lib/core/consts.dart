enum ScreenStatus { initial, loading, error, completed }

enum FirestoreCollections { recipes, user }

enum AppRoutes {
  home,
  login,
  search,
  favorite,
  newRecipe,
  dashboard,
  recipeDetails
}
